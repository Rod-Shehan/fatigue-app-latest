/**
 * Interim checklist pack custody via email (owner 2026-08-01).
 * Per-client distribution / packing cadence comes later; Circadia mailbox is the holding copy for now.
 * R2 / object-store photo offload is parked — see checklist-photo-r2-parked.md
 *
 * Packing doctrine (2026-08-01): one PDF = one checklist type for one driver week.
 * Do not merge FFW + Prestart + Dimension & Load (different regs; auditors call them up separately).
 * Fatigue roadside PDF is never included.
 */

/** Circadia holding inbox until per-client email distribution ships. */
export const CHECKLIST_ARCHIVE_EMAIL = "circadia24@gmail.com";

export const CHECKLIST_EMAIL_BUTTON_LABEL = "Email checklist week packs";

export const CHECKLIST_EMAIL_HINT =
  "Emails separate week PDFs per checklist type (FFW / Prestart / Load) to Circadia. Not the 28-day fatigue roadside PDF. Types are not combined.";
