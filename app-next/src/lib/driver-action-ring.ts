/**
 * Decorative ring mask for DriverActionHero.
 * Asset should be a white/light ring on a transparent background (PNG or SVG).
 * Drop your PNG at `public/driver-action-ring.png` and set the path below.
 */
export const DRIVER_ACTION_RING_MASK_PATH = "/driver-action-ring.png";

/** Fallback until a custom PNG is added to public/. */
export const DRIVER_ACTION_RING_MASK_FALLBACK_PATH = "/driver-action-ring.svg";
