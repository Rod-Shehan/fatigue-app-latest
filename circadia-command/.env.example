# Neon Postgres (shared instance)
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.region.aws.neon.tech/DB?sslmode=require"
DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@HOST.region.aws.neon.tech/DB?sslmode=require"

COMMAND_SESSION_SECRET="generate-with-openssl-rand-base64-32"
COMMAND_PILOT_TENANT_ID_UUID="00000000-0000-4000-8000-000000000001"

# Bootstrap first owner (run once: npm run bootstrap:owner)
# OPERATOR_USERNAME="your.name"
# OPERATOR_PASSWORD="change-me-min-12-chars"
# OPERATOR_FULL_NAME="Your Name"

COMMAND_OPERATOR_IP_WHITELIST=""
COMMAND_ALLOW_SIMULATE="false"

# Web Push (background triage alerts) — generate: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:command@circadia24.com"

# --- Nightly Neon backup → R2 (GitHub Actions only — not required for Command runtime) ---
# Shared Circadia Neon is dumped encrypted to a dedicated R2 bucket.
# See ../app-next/docs/ops/db-backup-restore.md
# GitHub Actions secrets: DATABASE_URL_UNPOOLED, BACKUP_ENCRYPTION_KEY,
#   R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME
